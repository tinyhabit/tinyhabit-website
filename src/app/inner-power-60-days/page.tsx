import React from 'react';
import InnerPowerPage, { metadata as originalMetadata } from '../inner-power/page';

export const metadata = originalMetadata;

export default function Page() {
  return <InnerPowerPage />;
}
